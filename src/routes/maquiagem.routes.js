import { Router} from "express";
import {maquiagemService} from '../services/maquiagem.service'

export const maquiagemRoutes = routes()

maquiagemRoutes.get ("/", async(req, res) =>{
try{
    const result = await

}
catch(error)










)